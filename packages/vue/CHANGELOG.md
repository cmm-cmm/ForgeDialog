# Changelog

## 0.1.0

- First release as its own package. The code is the adapter previously published as
  `forgedialog/vue` in `forgedialog@0.7.0`, unchanged in behaviour; only its home moved, so the
  core no longer declares a peer dependency on a framework most projects using it do not have.
